import FinancialState from './FinancialState.js';
import runSimulation from './Simulation.js';

// in this file, retrieve the user's selected scenario and run the specified number of simulations on that scenario
// run the simulations in this file

export default function Algorithm() {

    /* 

    Request simulation page data: selected scenario data, and number of simulations to run

    for (i = 0; i <= numSimulations; i++) {
        runSimulation(selectedScenarioData)
    }
    
    */
    return;
}